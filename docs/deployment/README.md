# Deployment i rollback

Ovde se dokumentuju:

- lokalno pokretanje;
- build i objavljivanje Docker image-a;
- Helm instalacija;
- canary koraci `10% -> 50% -> 100%`;
- kriterijumi uspeha i automatskog rollback-a;
- ručni rollback;
- opciona backup/restore procedura.

